# consummerApiEasyBroken

project Java
install JDK Java 11
build And run
exploye port 8080

before executing it is necessary to configure the easybroken.api.key property in the application.properties file, with its private key.

endpoint, properties list use:  /<page>
